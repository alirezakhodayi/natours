export interface ITour {
  id: number;
  heading: string;
  imageSrc: string;
  tourDuration: number;
  maxPeople: number;
  numberOfGuides: number;
  location: string;
  difficulty: string;
  lightColor: string;
  darkColor: string;
}
